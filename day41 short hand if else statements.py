# there is also a shorthand syntax for thr if ilse statement that can be used when the condition
# being tested is simpe and the code blocks to be executed are short



a=330
b=3303
print("a") if a>b else print("=") if a==b else print('b')